import { PrettyListPipe } from './pretty-list.pipe';

describe('PrettyListPipe', () => {
  it('create an instance', () => {
    const pipe = new PrettyListPipe();
    expect(pipe).toBeTruthy();
  });
});
