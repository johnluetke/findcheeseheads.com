import { FormatAddressDirective } from './format-address.directive';

describe('FormatAddressDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatAddressDirective();
    expect(directive).toBeTruthy();
  });
});
